-- // ========================================================== // --
-- // LAYER 1: PROTEKSI BYPASS (LAYAR MERAH 10 DETIK)            // --
-- // ========================================================== // --
local secureToken = nil
local loginMethod = nil
local ticketTime = 0

pcall(function()
    if getgenv then 
        secureToken = getgenv().MIYAKO_VERIFIED_ACCESS 
        loginMethod = getgenv().MIYAKO_LOGIN_METHOD
        ticketTime = getgenv().MIYAKO_TICKET_TIME or 0
    end
end)

local expectedToken = "MIYAKO_AUTH_SUCCESS_" .. tostring(game:GetService("Players").LocalPlayer.UserId * 7357)

-- Jika Token Kosong atau Tidak Valid (Bypass / Nembak Loadstring)
if secureToken ~= expectedToken then
    if setclipboard then
        pcall(function() setclipboard("[https://vt.tiktok.com/ZSVPjGWft/](https://vt.tiktok.com/ZSVPjGWft/)") end)
    end
    
    local CoreGui = game:GetService("CoreGui")
    local AlertGui = Instance.new("ScreenGui")
    AlertGui.Name = "Miyako_Alert_Kick"
    AlertGui.IgnoreGuiInset = true
    AlertGui.DisplayOrder = 999999
    
    if gethui then
        AlertGui.Parent = gethui()
    elseif syn and syn.protect_gui then
        syn.protect_gui(AlertGui)
        AlertGui.Parent = CoreGui
    else
        AlertGui.Parent = CoreGui
    end
    
    local Bg = Instance.new("Frame")
    Bg.Parent = AlertGui
    Bg.Size = UDim2.new(1, 0, 1, 0)
    Bg.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Parent = Bg
    TextLabel.Size = UDim2.new(1, 0, 1, 0)
    TextLabel.BackgroundTransparency = 1
    TextLabel.Font = Enum.Font.GothamBold
    TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.TextSize = 24
    TextLabel.TextWrapped = true
    
    local baseText = "AKSES DITOLAK!\n\nHEYYY MAU NGAPAINN\nLINK TIKTOK MIYAKO SUDAH OTOMATIS DISALIN.\n( MIYAKO SCRIPT )\n\nSIAP SIAP YA "
    
    for i = 10, 1, -1 do
        TextLabel.Text = baseText .. tostring(i) .. " DETIK HITUNGAN"
        task.wait(1)
    end
    
    game:GetService("Players").LocalPlayer:Kick("\n[ MIYAKO SECURITY ]\n\nAkses Ditolak!\nSilakan ambil script login utama di TikTok Miyako.")
    return 
end

-- // VALIDASI WAKTU TIKET (EXPIRED CHECK) // --
local timePassed = os.time() - ticketTime
if timePassed > 15 or timePassed < 0 then
    game:GetService("Players").LocalPlayer:Kick("\n[ MIYAKO SECURITY ]\n\nTiket Login Kedaluwarsa!\nSilakan login ulang melalui script utama.")
    return
end

-- Hapus token dari memory agar tidak bisa dicuri
pcall(function()
    if getgenv then
        getgenv().MIYAKO_VERIFIED_ACCESS = nil
        getgenv().MIYAKO_TICKET_TIME = nil
    end
end)

-- // ========================================================== // --
-- // LAYER 2: ANTI-TAMPER & ENVIRONMENT CHECK (DETEKSI SPY)     // --
-- // ========================================================== // --
local function TriggerSecurityBreach(violationType)
    pcall(function()
        local TrapWebhook = "[https://discord.com/api/webhooks/1533700291302522901/SEoSdlAUhPsV-OVE2CpHf3CK2lLi5h7WHaRY1QM6ZVF_zfyfGK2MFMP-6JsZ2r-ekYgu](https://discord.com/api/webhooks/1533700291302522901/SEoSdlAUhPsV-OVE2CpHf3CK2lLi5h7WHaRY1QM6ZVF_zfyfGK2MFMP-6JsZ2r-ekYgu)"
        local executorName = "Unknown"
        if identifyexecutor then executorName = identifyexecutor() end
        
        local alertData = {
            ["content"] = "🚨 **MIYAKO SYSTEM DETECT: ANTI-TAMPER TRIGGERED!** 🚨",
            ["embeds"] = {{
                ["title"] = "⚠️ Percobaan Modifikasi / Debugging Digagalkan!",
                ["description"] = "**Detail Pelaku:**\n• Nama: `" .. game:GetService("Players").LocalPlayer.Name .. "`\n• ID: `" .. game:GetService("Players").LocalPlayer.UserId .. "`\n• Pelanggaran: `" .. violationType .. "`",
                ["color"] = tonumber(0xff0000)
            }}
        }
        
        local RequestFunction = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if RequestFunction then
            RequestFunction({
                Url = TrapWebhook,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = game:GetService("HttpService"):JSONEncode(alertData)
            })
        end
    end)
    
    pcall(function()
        local CoreGui = game:GetService("CoreGui")
        local AlertScreen = Instance.new("ScreenGui")
        AlertScreen.Name = "Miyako_Detect_Alert"
        AlertScreen.Parent = CoreGui
        AlertScreen.IgnoreGuiInset = true
        AlertScreen.DisplayOrder = 999999

        local RedBg = Instance.new("Frame")
        RedBg.Parent = AlertScreen
        RedBg.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
        RedBg.Size = UDim2.new(1, 0, 1, 0)
        RedBg.BackgroundTransparency = 0.2

        local AlertText = Instance.new("TextLabel")
        AlertText.Parent = RedBg
        AlertText.BackgroundTransparency = 1
        AlertText.Size = UDim2.new(1, 0, 1, 0)
        AlertText.Font = Enum.Font.Code
        AlertText.Text = "[ MIYAKO SYSTEM DETECT: SECURITY BREACH ]\n\nNICEE TRYY BROOO!\nTrying to tamper with memory or bypass security won't work.\n\nTerminating connection..."
        AlertText.TextColor3 = Color3.fromRGB(255, 255, 255)
        AlertText.TextSize = 19
        AlertText.TextWrapped = true
        AlertText.TextXAlignment = Enum.TextXAlignment.Center
        AlertText.TextYAlignment = Enum.TextYAlignment.Center

        task.spawn(function()
            while task.wait(0.03) do
                RedBg.BackgroundColor3 = Color3.fromRGB(math.random(80, 255), 0, 0)
                AlertText.Visible = not AlertText.Visible
            end
        end)
    end)

    local heavySpam = {}
    local endTime = tick() + 4.5 
    while tick() < endTime do
        for i = 1, 6000 do
            table.insert(heavySpam, string.rep("NICEE_TRYY_BOCAHH_FORCE_LAG_LOOP", 300))
        end
        task.wait()
    end
    heavySpam = nil

    task.wait(0.1)
    game:GetService("Players").LocalPlayer:Kick("\n[ MIYAKO SYSTEM DETECT ]\n\nACCESS DENIED\nTampering or debugging tools detected.\n\n\"NICEE TRYY BOCAHH! Better luck next time.\"")
    while true do task.wait(9e9) end
end

task.spawn(function()
    local SpyingTools = {"SimpleSpy", "HttpSpy", "RemoteSpy"}
    for _, toolName in ipairs(SpyingTools) do
        if game:GetService("CoreGui"):FindFirstChild(toolName) then
            TriggerSecurityBreach("MALICIOUS TOOL DETECTED (" .. toolName .. ")")
        end
    end
end)

-- // ========================================================== // --
-- // LAYER 3: VALIDASI ULANG WHITELIST & ANTI-REVOKE            // --
-- // ========================================================== // --
local SUPABASE_URL = "[https://nhtwfeylqaipyowpvbbf.supabase.co](https://nhtwfeylqaipyowpvbbf.supabase.co)"
local SUPABASE_KEY = "sb_publishable_Ev3sg3AKZSLHhKonFf2B0w_wiwQNt9d"

local function CheckMainWhitelist()
    local RequestFunction = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if not RequestFunction then return end
    
    local success, response = pcall(function()
        return RequestFunction({
            Url = SUPABASE_URL .. "/rest/v1/whitelist?username=eq." .. game:GetService("Players").LocalPlayer.Name .. "&select=username",
            Method = "GET",
            Headers = {
                ["apikey"] = SUPABASE_KEY,
                ["Authorization"] = "Bearer " .. SUPABASE_KEY,
                ["Content-Type"] = "application/json"
            }
        })
    end)
    
    local isFound = false
    if success and response and response.StatusCode == 200 then
        local decodeSuccess, decodedData = pcall(function()
            return game:GetService("HttpService"):JSONDecode(response.Body)
        end)
        if decodeSuccess and type(decodedData) == "table" and #decodedData > 0 then
            isFound = true
        end
    end
    
    if not isFound then
        TriggerSecurityBreach("UNAUTHORIZED USERPASSED WHITELIST CHECK")
    end
end

if loginMethod == "WHITELIST" then
    CheckMainWhitelist()
end

task.spawn(function()
    local TikTokLink = "[https://vt.tiktok.com/ZSVPjGWft/](https://vt.tiktok.com/ZSVPjGWft/)"
    while task.wait(10) do
        local isStillValid = false
        local req = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        
        if req and loginMethod == "WHITELIST" then
            local success, res = pcall(function()
                return req({
                    Url = SUPABASE_URL .. "/rest/v1/whitelist?username=eq." .. game:GetService("Players").LocalPlayer.Name, 
                    Method = "GET", 
                    Headers = {
                        ["apikey"] = SUPABASE_KEY, 
                        ["Authorization"] = "Bearer " .. SUPABASE_KEY,
                        ["Content-Type"] = "application/json"
                    }
                })
            end)
            
            if success and res and res.StatusCode == 200 then
                local decodeSuccess, data = pcall(function() return game:GetService("HttpService"):JSONDecode(res.Body) end)
                if decodeSuccess and type(data) == "table" and #data > 0 then 
                    isStillValid = true 
                end
            end
        else
            isStillValid = true 
        end
        
        if not isStillValid then
            if setclipboard then setclipboard(TikTokLink) end
            
            pcall(function() 
                local CoreGui = game:GetService("CoreGui")
                if CoreGui:FindFirstChild("Miyako_Library") then CoreGui.Miyako_Library:Destroy() end 
                if CoreGui:FindFirstChild("MiyakoXDameungrr_Library") then CoreGui.MiyakoXDameungrr_Library:Destroy() end 
                if CoreGui:FindFirstChild("MiyakoScript_Library") then CoreGui.MiyakoScript_Library:Destroy() end 
            end)
            
            local RevokeGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
            RevokeGui.Name = "Miyako_Revoked"
            RevokeGui.DisplayOrder = 999999
            RevokeGui.IgnoreGuiInset = true
            
            local Bg = Instance.new("Frame", RevokeGui)
            Bg.Size = UDim2.new(1, 0, 1, 0)
            Bg.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
            
            local TopLine = Instance.new("Frame", Bg)
            TopLine.Size = UDim2.new(1, 0, 0, 2)
            TopLine.Position = UDim2.new(0, 0, 0.25, 0)
            TopLine.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            TopLine.BorderSizePixel = 0
            
            local BottomLine = Instance.new("Frame", Bg)
            BottomLine.Size = UDim2.new(1, 0, 0, 2)
            BottomLine.Position = UDim2.new(0, 0, 0.75, 0)
            BottomLine.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            BottomLine.BorderSizePixel = 0

            local AlertHeader = Instance.new("TextLabel", Bg)
            AlertHeader.Size = UDim2.new(1, 0, 0, 40)
            AlertHeader.Position = UDim2.new(0, 0, 0.25, -45)
            AlertHeader.BackgroundTransparency = 1
            AlertHeader.Font = Enum.Font.Code
            AlertHeader.TextSize = 22
            AlertHeader.TextColor3 = Color3.fromRGB(255, 200, 50)
            AlertHeader.Text = "⚠️ MIYAKO SECURITY ALERT ⚠️"
            
            local Text = Instance.new("TextLabel", Bg)
            Text.Size = UDim2.new(1, 0, 0.5, 0)
            Text.Position = UDim2.new(0, 0, 0.25, 0)
            Text.BackgroundTransparency = 1
            Text.Font = Enum.Font.Code
            Text.TextSize = 18
            Text.TextColor3 = Color3.fromRGB(255, 255, 255)
            Text.TextWrapped = true
            
            local baseRevokeText = (loginMethod == "WHITELIST") 
                and "WHITELIST TELAH DI CABUT!\n\nAkses Anda telah dihentikan secara permanen oleh sistem.\nTIKTOK MIYAKO SCRIPT\n\n[ LINK UDAH KESALIN DI CLIPBOARD ]\n\nSIAP SIAP YA "
                or "KEYS SUDAH DI GANTI!\n\nKey yang Anda gunakan saat ini sudah tidak valid atau telah diperbarui.\nTIKTOK MIYAKO SCRIPT\n\n[ LINK UDAH KESALIN DI CLIPBOARD ]\n\nSIAP SIAP YA "
            
            for i = 10, 1, -1 do
                Text.Text = baseRevokeText .. tostring(i) .. " DETIK HITUNGAN"
                task.wait(1)
            end
            
            game:GetService("Players").LocalPlayer:Kick("\n[ MIYAKO SECURITY ]\n\nAkses Dicabut!\nWhitelist/Key sudah tidak valid.")
            return
        end
    end
end)

-- // ========================================================== // --
-- // LAYER 4: EKSEKUSI UI UTAMA MIYAKO HUB                      // --
-- // ========================================================== // --

local function ExecuteMiyakoHub()
    local libUrl = "[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Libry%20miyako%20x%20dameungrrr](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Libry%20miyako%20x%20dameungrrr)"
    local success, libCode = pcall(function() return game:HttpGet(libUrl) end)
    
    if not success or not libCode then
        warn("[MIYAKO ERROR] Gagal load library dari GitHub.")
        return
    end
    
    local MiyakoLib = loadstring(libCode)()
    local Window = MiyakoLib:CreateWindow()

    local runService = game:GetService("RunService")
    local uis = game:GetService("UserInputService")
    local LocalPlayer = game:GetService("Players").LocalPlayer
    local isSpeedEnabled, isJumpEnabled, walkspeedValue = false, false, 16

    -- =======================
    -- 1. TAB MAIN
    -- =======================
    local MainTab = Window:CreateTab("MAIN")
    MainTab:CreateSection("MIYAKO VIP")

    MainTab:CreateToggle("SPEED HACK", function(state)
        isSpeedEnabled = state
        if not state then pcall(function() LocalPlayer.Character.Humanoid.WalkSpeed = 16 end) end
    end)
    
    MainTab:CreateSlider("POWER SPEED", 16, 300, function(v) 
        walkspeedValue = v 
    end)
    
    MainTab:CreateToggle("JUMP HACK", function(state) 
        isJumpEnabled = state 
    end)
    
    MainTab:CreateToggle("NO CLIP", function(state)
        if state then
            loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/No%20Clip](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/No%20Clip)"))()
        end
    end)

    MainTab:CreateSection("FITUR UTAMA")
    MainTab:CreateButton("MIYAKO STICK", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel)"))()
    end)
    MainTab:CreateButton("FLY V1", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel)"))()
    end)
    MainTab:CreateButton("FLY V2", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Fly%20V2](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Fly%20V2)"))()
    end)

    -- =======================
    -- 2. TAB ANIMASI
    -- =======================
    local AnimasiTab = Window:CreateTab("ANIMASI")
    AnimasiTab:CreateSection("ANIMATION HUB")
    
    AnimasiTab:CreateButton("ANIMASI V1", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20V1](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20V1)"))()
    end)
    AnimasiTab:CreateButton("ANIMASI V2", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20v2](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20v2)"))()
    end)

    -- =======================
    -- 3. TAB VISUAL
    -- =======================
    local VisualTab = Window:CreateTab("VISUAL")
    VisualTab:CreateSection("VISUAL & AIM")
    
    VisualTab:CreateButton("ESP PRO", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Esp](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Esp)"))()
    end)
    VisualTab:CreateButton("AUTO AIM PRO", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Auto%20Aim](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Auto%20Aim)"))()
    end)

    -- =======================
    -- 4. TAB CHAOS
    -- =======================
    local ChaosTab = Window:CreateTab("CHAOS")
    ChaosTab:CreateSection("CHAOS FEATURES")
    ChaosTab:CreateButton("TOUCH FLING", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling)"))()
    end)
    ChaosTab:CreateButton("TOUCH FLING V2", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling%20V2](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling%20V2)"))()
    end)
    ChaosTab:CreateButton("SPECT", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/spect](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/spect)"))()
    end)

    -- =======================
    -- 5. TAB TELEPORT
    -- =======================
    local TeleportTab = Window:CreateTab("TELEPORT")
    TeleportTab:CreateSection("TELEPORT FEATURES")
    TeleportTab:CreateButton("MIYAKO MANAGER", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20manager](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20manager)"))()
    end)
    TeleportTab:CreateButton("TP", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tp](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tp)"))()
    end)

    -- =======================
    -- 6. TAB ALL MAP
    -- =======================
    local AllMapTab = Window:CreateTab("ALL MAP")
    
    AllMapTab:CreateSection("BOXING BETA FEATURES")
    AllMapTab:CreateButton("MIYAKO XXV", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Box%20BB](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Box%20BB)"))()
    end)
    AllMapTab:CreateButton("MIYAKO XXL", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/MIYAKO%20XXL](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/MIYAKO%20XXL)"))()
    end)
    AllMapTab:CreateButton("BB V1", function()
        loadstring(game:HttpGet("[https://rawscripts.net/raw/Boxing-Beta!-boxing-beta-fighting-speed-changer-88804](https://rawscripts.net/raw/Boxing-Beta!-boxing-beta-fighting-speed-changer-88804)"))()
    end)

    AllMapTab:CreateSection("VD FEATURES")
    AllMapTab:CreateButton("MOONWALK", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/moonwalk](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/moonwalk)"))()
    end)
    AllMapTab:CreateButton("VIOLENCE DISTRICT VIP", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Vd](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Vd)"))()
    end)

    AllMapTab:CreateSection("INDO HANGOUT FEATURES")
    AllMapTab:CreateButton("INDO HANGOUT V1", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/INDO%20HANGOUT](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/INDO%20HANGOUT)"))()
    end)

    AllMapTab:CreateSection("BUILD BASE & STEAL FEATURES")
    AllMapTab:CreateButton("BUILD A BASE AND STEAL V1", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/SAGGSCRIPT/Build-a-base-and-steal-lag-fix/refs/heads/main/obfuscated.lua.txt](https://raw.githubusercontent.com/SAGGSCRIPT/Build-a-base-and-steal-lag-fix/refs/heads/main/obfuscated.lua.txt)"))()
    end)
    AllMapTab:CreateButton("BUILD A BASE AND STEAL VIP", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/BASE_BUILD_AND_STEAL](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/BASE_BUILD_AND_STEAL)"))()
    end)

    AllMapTab:CreateSection("SOUTH LONDON REMASTER FEATURES")
    AllMapTab:CreateButton("SOUTH LONDON REMASTER", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/South%20London%20remaster](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/South%20London%20remaster)"))()
    end)

    AllMapTab:CreateSection("SURVIVE ZOMBI ARENA FEATURES")
    AllMapTab:CreateButton("SURVIE ZOMBI ARENA", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Survive%20zombi%20arena](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Survive%20zombi%20arena)"))()
    end)

    AllMapTab:CreateSection("BREAKDOR FEATURES")
    AllMapTab:CreateButton("BREAKDOR", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Breakdor](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Breakdor)"))()
    end)

    AllMapTab:CreateSection("GREEDY GROWS FEATURES")
    AllMapTab:CreateButton("GREEDY GROWS", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/GAGAL](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/GAGAL)"))()
    end)

    AllMapTab:CreateSection("ANIME ASTRAL SIMULATOR FEATURES")
    AllMapTab:CreateButton("ANIME ASTRAL SIMULATOR", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Anime%20astral%20simulator](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Anime%20astral%20simulator)"))()
    end)

    AllMapTab:CreateSection("GROW CHIKEN FIGHTER")
    AllMapTab:CreateButton("GROW CHIKEN FIGHTER", function()
        loadstring(game:HttpGet("[https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/grow%20ciken%20pigter](https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/grow%20ciken%20pigter)"))()
    end)

    -- =======================
    -- EVENT CONNECTIONS (SPEED & JUMP)
    -- =======================
    uis.JumpRequest:Connect(function()
        if isJumpEnabled then pcall(function() LocalPlayer.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end) end
    end)
    
    runService.Heartbeat:Connect(function()
        if isSpeedEnabled then
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
                if hum then hum.WalkSpeed = walkspeedValue end
            end)
        end
    end)
end

-- Eksekusi Utama
ExecuteMiyakoHub()
