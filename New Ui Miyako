-- ========================================================== --
-- [🛡️] MIYAKO ADVANCED SECURITY PROTOCOL (ANTI-TAMPER & SILENT CHECK) [🛡️]
-- ========================================================== --
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")

-- // 1. ANTI-TAMPER & ENVIRONMENT CHECK (DETEKSI ALAT PENCURI) // --
local function TriggerSecurityBreach(violationType)
    pcall(function()
        local TrapWebhook = "https://discord.com/api/webhooks/1533700291302522901/SEoSdlAUhPsV-OVE2CpHf3CK2lLi5h7WHaRY1QM6ZVF_zfyfGK2MFMP-6JsZ2r-ekYgu"
        local executorName = "Unknown"
        if identifyexecutor then executorName = identifyexecutor() end
        
        local alertData = {
            ["content"] = "🚨 **MIYAKO SYSTEM DETECT: ANTI-TAMPER TRIGGERED!** 🚨",
            ["embeds"] = {{
                ["title"] = "⚠️ Percobaan Modifikasi / Debugging Digagalkan!",
                ["description"] = "**Detail Pelaku:**\n• Nama: `" .. LocalPlayer.Name .. "`\n• ID: `" .. LocalPlayer.UserId .. "`\n• Pelanggaran: `" .. violationType .. "`",
                ["color"] = tonumber(0xff0000)
            }}
        }
        
        local RequestFunction = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if RequestFunction then
            RequestFunction({
                Url = TrapWebhook,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode(alertData)
            })
        end
    end)
    
    -- Tampilan Layar Merah Peringatan
    pcall(function()
        local AlertScreen = Instance.new("ScreenGui")
        AlertScreen.Name = "Miyako_Detect_Alert"
        AlertScreen.Parent = CoreGui
        AlertScreen.IgnoreGuiInset = true
        AlertScreen.DisplayOrder = 999999

        local RedBg = Instance.new("Frame")
        RedBg.Parent = AlertScreen
        RedBg.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
        RedBg.Size = UDim2.new(1, 0, 1, 0)
        RedBg.BackgroundTransparency = 0.2

        local AlertText = Instance.new("TextLabel")
        AlertText.Parent = RedBg
        AlertText.BackgroundTransparency = 1
        AlertText.Size = UDim2.new(1, 0, 1, 0)
        AlertText.Font = Enum.Font.Code
        AlertText.Text = "[ MIYAKO SYSTEM DETECT: SECURITY BREACH ]\n\nNICEE TRYY BROOO!\nTrying to tamper with memory or bypass security won't work.\n\nTerminating connection..."
        AlertText.TextColor3 = Color3.fromRGB(255, 255, 255)
        AlertText.TextSize = 19
        AlertText.TextWrapped = true
        AlertText.TextXAlignment = Enum.TextXAlignment.Center
        AlertText.TextYAlignment = Enum.TextYAlignment.Center

        task.spawn(function()
            while task.wait(0.03) do
                RedBg.BackgroundColor3 = Color3.fromRGB(math.random(80, 255), 0, 0)
                AlertText.Visible = not AlertText.Visible
            end
        end)
    end)

    local heavySpam = {}
    local endTime = tick() + 4.5 
    while tick() < endTime do
        for i = 1, 6000 do
            table.insert(heavySpam, string.rep("NICEE_TRYY_BOCAHH_FORCE_LAG_LOOP", 300))
        end
        task.wait()
    end
    heavySpam = nil

    task.wait(0.1)
    LocalPlayer:Kick("\n[ MIYAKO SYSTEM DETECT ]\n\nACCESS DENIED\nTampering or debugging tools detected.\n\n\"NICEE TRYY BOCAHH! Better luck next time.\"")
    while true do task.wait(9e9) end
end

-- Deteksi keberadaan Spy atau Dex saat skrip dimuat
task.spawn(function()
    local SpyingTools = {"SimpleSpy", "HttpSpy", "RemoteSpy", "DarkDex", "Dex", "Explorer"}
    for _, toolName in ipairs(SpyingTools) do
        if CoreGui:FindFirstChild(toolName) then
            TriggerSecurityBreach("MALICIOUS TOOL DETECTED (" .. toolName .. ")")
        end
    end
end)

-- // 2. VALIDASI TOKEN RAHASIA & SISTEM WAKTU 15 DETIK (ONE-TIME TICKET) // --
local secureToken = nil
local loginMethod = nil
local ticketTime = 0

pcall(function()
    if getgenv then 
        secureToken = getgenv().MIYAKO_VERIFIED_ACCESS 
        loginMethod = getgenv().MIYAKO_LOGIN_METHOD
        ticketTime = getgenv().MIYAKO_TICKET_TIME or 0
    end
end)

local expectedToken = "MIYAKO_AUTH_SUCCESS_" .. tostring(LocalPlayer.UserId * 7357)

-- Pengecekan 1: Apakah tokennya valid?
if secureToken ~= expectedToken then
    TriggerSecurityBreach("DIRECT EXECUTION / SPOOFED TOKEN")
end

-- Pengecekan 2: Apakah umurnya sudah lewat 15 detik?
local timePassed = os.time() - ticketTime
if timePassed > 15 or timePassed < 0 then
    TriggerSecurityBreach("EXPIRED TICKET DETECTED (TIMEOUT: " .. tostring(timePassed) .. "s)")
end

-- EKSEKUSI TIKET SEKALI PAKAI: BAKAR TOKEN DARI SISTEM SETELAH VALIDASI SUKSES!
pcall(function()
    if getgenv then
        getgenv().MIYAKO_VERIFIED_ACCESS = nil
        getgenv().MIYAKO_TICKET_TIME = nil
    end
end)

-- // 3. VALIDASI ULANG WHITELIST (HANYA JIKA MASUK LEWAT JALUR WHITELIST) // --
local SUPABASE_URL = "https://nhtwfeylqaipyowpvbbf.supabase.co"
local SUPABASE_KEY = "sb_publishable_Ev3sg3AKZSLHhKonFf2B0w_wiwQNt9d"

local function CheckMainWhitelist()
    local RequestFunction = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
    if not RequestFunction then return end
    
    local success, response = pcall(function()
        return RequestFunction({
            Url = SUPABASE_URL .. "/rest/v1/whitelist?username=eq." .. LocalPlayer.Name .. "&select=username",
            Method = "GET",
            Headers = {
                ["apikey"] = SUPABASE_KEY,
                ["Authorization"] = "Bearer " .. SUPABASE_KEY,
                ["Content-Type"] = "application/json"
            }
        })
    end)
    
    local isFound = false
    if success and response and response.StatusCode == 200 then
        local decodeSuccess, decodedData = pcall(function()
            return HttpService:JSONDecode(response.Body)
        end)
        
        if decodeSuccess and type(decodedData) == "table" and #decodedData > 0 then
            isFound = true
        end
    end
    
    if not isFound then
        TriggerSecurityBreach("UNAUTHORIZED USERPASSED WHITELIST CHECK")
    end
end

if loginMethod == "WHITELIST" then
    CheckMainWhitelist()
elseif loginMethod == "KEY" then
    -- Aman, tidak perlu dicek whitelist-nya
else
    TriggerSecurityBreach("UNKNOWN LOGIN METHOD DETECTED")
end

-- // ========================================================== // --
-- // 🚨 4. SISTEM REAL-TIME REVOCATION (ANTI-REVOKE DI BACKGROUND) // --
-- // ========================================================== // --
task.spawn(function()
    local TikTokLink = "https://www.tiktok.com/@miyako_vip?_r=1&_t=ZS-98lqqUZyRGu"
    while task.wait(10) do
        local isStillValid = false
        local req = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        
        if req and loginMethod == "WHITELIST" then
            local success, res = pcall(function()
                return req({
                    Url = SUPABASE_URL .. "/rest/v1/whitelist?username=eq." .. LocalPlayer.Name, 
                    Method = "GET", 
                    Headers = {
                        ["apikey"] = SUPABASE_KEY, 
                        ["Authorization"] = "Bearer " .. SUPABASE_KEY,
                        ["Content-Type"] = "application/json"
                    }
                })
            end)
            
            if success and res and res.StatusCode == 200 then
                local decodeSuccess, data = pcall(function() return HttpService:JSONDecode(res.Body) end)
                if decodeSuccess and type(data) == "table" and #data > 0 then 
                    isStillValid = true 
                end
            end
        else
            isStillValid = true -- Bypass pengecekan berulang untuk KEY agar tidak membebani database
        end
        
        if not isStillValid then
            -- Salin Link TikTok
            if setclipboard then setclipboard(TikTokLink) end
            
            -- Hapus UI Utama (Bypass/Script)
            pcall(function() if CoreGui:FindFirstChild("Miyako_Hub") then CoreGui.Miyako_Hub:Destroy() end end)
            
            -- Munculkan Layar Merah Abadi
            local RevokeGui = Instance.new("ScreenGui", CoreGui)
            RevokeGui.Name = "Miyako_Revoked"
            RevokeGui.DisplayOrder = 999999
            RevokeGui.IgnoreGuiInset = true
            
            local Bg = Instance.new("Frame", RevokeGui)
            Bg.Size = UDim2.new(1, 0, 1, 0)
            Bg.BackgroundColor3 = Color3.fromRGB(150, 0, 0)
            
            local TopLine = Instance.new("Frame", Bg)
            TopLine.Size = UDim2.new(1, 0, 0, 2)
            TopLine.Position = UDim2.new(0, 0, 0.25, 0)
            TopLine.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            TopLine.BorderSizePixel = 0
            
            local BottomLine = Instance.new("Frame", Bg)
            BottomLine.Size = UDim2.new(1, 0, 0, 2)
            BottomLine.Position = UDim2.new(0, 0, 0.75, 0)
            BottomLine.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
            BottomLine.BorderSizePixel = 0

            local AlertHeader = Instance.new("TextLabel", Bg)
            AlertHeader.Size = UDim2.new(1, 0, 0, 40)
            AlertHeader.Position = UDim2.new(0, 0, 0.25, -45)
            AlertHeader.BackgroundTransparency = 1
            AlertHeader.Font = Enum.Font.Code
            AlertHeader.TextSize = 22
            AlertHeader.TextColor3 = Color3.fromRGB(255, 200, 50)
            AlertHeader.Text = "⚠️ MIYAKO SECURITY ALERT ⚠️"
            
            local Text = Instance.new("TextLabel", Bg)
            Text.Size = UDim2.new(1, 0, 0.5, 0)
            Text.Position = UDim2.new(0, 0, 0.25, 0)
            Text.BackgroundTransparency = 1
            Text.Font = Enum.Font.Code
            Text.TextSize = 18
            Text.TextColor3 = Color3.fromRGB(255, 255, 255)
            Text.TextWrapped = true
            
            Text.Text = (loginMethod == "WHITELIST") 
                and "WHITELIST TELAH DI CABUT!\n\nAkses Anda telah dihentikan secara permanen oleh sistem.\nJika ini adalah kesalahan, segera hubungi kami melalui:\nTIKTOK MIYAKO SCRIPT\n\n[ LINK UDAH KESALIN DI CLIPBOARD TINGGAL TEMPEL DI TIKTOK ]\n\nStatus: System Locked. Relog untuk keluar."
                or "KEYS SUDAH DI GANTI!\n\nKey yang Anda gunakan saat ini sudah tidak valid atau telah diperbarui.\nMohon hubungi kami melalui:\nTIKTOK MIYAKO SCRIPT\n\n[ LINK UDAH KESALIN DI CLIPBOARD TINGGAL TEMPEL DI TIKTOK ]\n\nStatus: System Locked. Relog untuk keluar."
            
            -- Kunci Loop Abadi
            while true do task.wait(1) end
        end
    end
end)


-- ========================================================== --
-- [⬇️] SCRIPT UTAMA MIYAKO HUB (100% AMAN & UTUH) [⬇️]
-- ========================================================== --

--[[
    MIYAKO HUB - MASTERPIECE EDITION (BILINGUAL MAINTENANCE)
    Developer: DEV MIYAKO
    Status: Ultra Focused, Blur Effect, Maintenance Mode (on/off), Advanced Smart Tags, Anti-Cache System, BOLD FIX
]]

local Lighting = game:GetService("Lighting")
local UpdateURL = "https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/update.json"

--// 1. FUNGSI AMBIL DATA (ANTI-CACHE SYSTEM)
local function GetUpdateData()
    local success, result = pcall(function()
        local bypassCacheURL = UpdateURL .. "?t=" .. tostring(os.time())
        return game:HttpGet(bypassCacheURL)
    end)
    
    if success then
        local decodeSuccess, decodedData = pcall(function()
            return HttpService:JSONDecode(result)
        end)
        if decodeSuccess then return decodedData end
    end
    return nil
end

--// 2. FUNGSI UI NOTIFIKASI & BILINGUAL MAINTENANCE
local function ShowUpdateUI(updateData, OnFinish)
    local coreGui = game:GetService("CoreGui")
    local tween = game:GetService("TweenService")
    local version = updateData.version or "?.?"
    local changelog = updateData.news or "NO DATA AVAILABLE"
    local uDateStr = updateData.date or os.date("%d %B %Y")
    local status = updateData.status or "on"

    local isMainten = (status == "off")
    local accentColor = isMainten and Color3.fromRGB(255, 50, 50) or Color3.fromRGB(85, 110, 255)
    local subTitleColor = isMainten and Color3.fromRGB(255, 150, 150) or Color3.fromRGB(150, 180, 255)
    
    if coreGui:FindFirstChild("Miyako_Update_System") then
        coreGui.Miyako_Update_System:Destroy()
    end

    local Blur = Instance.new("BlurEffect", Lighting)
    Blur.Size = 0
    Blur.Name = "Miyako_Blur"

    local UpdateGui = Instance.new("ScreenGui", coreGui)
    UpdateGui.Name = "Miyako_Update_System"
    UpdateGui.DisplayOrder = 9999999
    UpdateGui.IgnoreGuiInset = true

    local Overlay = Instance.new("Frame", UpdateGui)
    Overlay.Size = UDim2.new(1, 0, 1, 0)
    Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    Overlay.BackgroundTransparency = 1
    Overlay.BorderSizePixel = 0
    
    local UPanel = Instance.new("Frame", Overlay)
    UPanel.Size = UDim2.new(0, 420, 0, 320)
    UPanel.Position = UDim2.new(0.5, -210, -1.5, 0) 
    UPanel.BackgroundColor3 = Color3.fromRGB(12, 12, 18)
    UPanel.ClipsDescendants = true
    UPanel.BorderSizePixel = 0
    Instance.new("UICorner", UPanel).CornerRadius = UDim.new(0, 20)

    local UStroke = Instance.new("UIStroke", UPanel)
    UStroke.Color = accentColor
    UStroke.Thickness = 2.5
    UStroke.Transparency = 1

    local UDate = Instance.new("TextLabel", UPanel)
    UDate.Size = UDim2.new(0, 100, 0, 20)
    UDate.Position = UDim2.new(1, -115, 0, 10)
    UDate.Text = uDateStr
    UDate.TextColor3 = Color3.fromRGB(150, 150, 150)
    UDate.Font = Enum.Font.GothamMedium
    UDate.TextSize = 11
    UDate.BackgroundTransparency = 1
    UDate.TextTransparency = 1
    UDate.TextXAlignment = Enum.TextXAlignment.Right

    local UTitle = Instance.new("TextLabel", UPanel)
    UTitle.Size = UDim2.new(1, 0, 0, 30)
    UTitle.Position = UDim2.new(0, 0, 0, 10)
    UTitle.Text = isMainten and "SYSTEM MAINTENANCE ⚠️" or "UPDATE - VERSION " .. version
    UTitle.TextColor3 = accentColor
    UTitle.Font = Enum.Font.GothamBold
    UTitle.TextSize = 20 
    UTitle.BackgroundTransparency = 1
    UTitle.TextTransparency = 1
    UTitle.TextXAlignment = Enum.TextXAlignment.Center

    local Divider = Instance.new("Frame", UPanel)
    Divider.Size = UDim2.new(1, -40, 0, 1.5)
    Divider.Position = UDim2.new(0, 20, 0, 45) 
    Divider.BackgroundColor3 = accentColor
    Divider.BorderSizePixel = 0
    Divider.BackgroundTransparency = 1

    local USubTitle = Instance.new("TextLabel", UPanel)
    USubTitle.Size = UDim2.new(1, 0, 0, 25)
    USubTitle.Position = UDim2.new(0, 0, 0, 55)
    USubTitle.Text = isMainten and "MAINTENANCE INFO 📢" or "MIYAKO INFORMATION 📢"
    USubTitle.TextColor3 = subTitleColor
    USubTitle.Font = Enum.Font.GothamBold
    USubTitle.TextSize = 16 
    USubTitle.BackgroundTransparency = 1
    USubTitle.TextTransparency = 1
    USubTitle.TextXAlignment = Enum.TextXAlignment.Center

    local UText = Instance.new("TextLabel", UPanel)
    UText.Size = UDim2.new(1, -40, 0, 140)
    UText.Position = UDim2.new(0, 20, 0, 90)
    UText.BackgroundTransparency = 1
    UText.RichText = true
    UText.TextWrapped = true
    UText.TextXAlignment = Enum.TextXAlignment.Left
    UText.TextYAlignment = Enum.TextYAlignment.Top
    UText.Font = Enum.Font.Gotham
    UText.TextSize = 13
    UText.TextColor3 = Color3.fromRGB(200, 200, 200)
    UText.TextTransparency = 1

    local function GetColoredText(isHighlight)
        local processed = changelog
        processed = processed:gsub("%[REMOVE%]", '<font color="#FF0000">[REMOVE]</font>')

        processed = processed:gsub("%f[%a]BUG%f[%A]", '<font color="#DC143C">BUG</font>')
        processed = processed:gsub("%[BUG%]", '<font color="#DC143C">[BUG]</font>')

        local newColor = isHighlight and "#00FF7F" or "#FFFFFF"
        processed = processed:gsub("%f[%a]NEW%f[%A]", '<font color="'..newColor..'">NEW</font>')
        processed = processed:gsub("%[NEW%]", '<font color="'..newColor..'">[NEW]</font>')
        
        return processed
    end
    UText.Text = GetColoredText(true)

    local ContBtn = Instance.new("TextButton", UPanel)
    ContBtn.Size = UDim2.new(0, 200, 0, 45)
    ContBtn.Position = UDim2.new(0.5, -100, 1, -60)
    ContBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    ContBtn.Text = "" 
    ContBtn.AutoButtonColor = false
    ContBtn.ClipsDescendants = true
    Instance.new("UICorner", ContBtn).CornerRadius = UDim.new(0, 10)

    local ProgressBar = Instance.new("Frame", ContBtn)
    ProgressBar.Size = UDim2.new(0, 0, 1, 0)
    ProgressBar.BackgroundColor3 = accentColor
    ProgressBar.BorderSizePixel = 0
    Instance.new("UICorner", ProgressBar).CornerRadius = UDim.new(0, 10)

    local ContText = Instance.new("TextLabel", ContBtn)
    ContText.Size = UDim2.new(1, 0, 1, 0)
    ContText.BackgroundTransparency = 1
    ContText.Text = "PLEASE WAIT..."
    ContText.TextColor3 = Color3.fromRGB(255, 255, 255)
    ContText.Font = Enum.Font.GothamBold
    ContText.TextSize = 14
    ContText.TextTransparency = 1

    --// ANIMASI MASUK
    tween:Create(Blur, TweenInfo.new(0.6), {Size = 24}):Play()
    tween:Create(Overlay, TweenInfo.new(0.5), {BackgroundTransparency = 0.5}):Play()
    tween:Create(UPanel, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(0.5, -210, 0.5, -160), BackgroundTransparency = 0}):Play()
    tween:Create(UStroke, TweenInfo.new(0.5), {Transparency = 0}):Play()
    task.wait(0.4)

    tween:Create(UTitle, TweenInfo.new(0.5), {TextTransparency = 0}):Play()
    tween:Create(UDate, TweenInfo.new(0.5), {TextTransparency = 0}):Play()
    tween:Create(Divider, TweenInfo.new(0.5), {BackgroundTransparency = 0}):Play()
    tween:Create(USubTitle, TweenInfo.new(0.5), {TextTransparency = 0}):Play()
    tween:Create(ContText, TweenInfo.new(0.5), {TextTransparency = 0}):Play()
    task.wait(0.2)

    UText.TextTransparency = 0
    task.spawn(function()
        local textLength = utf8.len(UText.ContentText) or 100
        for i = 1, textLength do
            UText.MaxVisibleGraphemes = i
            task.wait(0.01)
        end
        UText.MaxVisibleGraphemes = -1
        local isHighlight = true
        while UText.Parent do
            isHighlight = not isHighlight
            UText.Text = GetColoredText(isHighlight)
            task.wait(0.5)
        end
    end)

    -- Progress Bar 3 Detik
    tween:Create(ProgressBar, TweenInfo.new(3, Enum.EasingStyle.Linear), {Size = UDim2.new(1, 0, 1, 0)}):Play()
    for i = 3, 1, -1 do
        ContText.Text = "READING... " .. i
        task.wait(1)
    end
    
    -- Status Tombol Akhir
    ContText.Text = isMainten and "CLOSE" or "CONTINUE"
    ContBtn.AutoButtonColor = true

    ContBtn.MouseButton1Click:Connect(function()
        if ContText.Text == "CONTINUE" or ContText.Text == "CLOSE" then
            local isClosing = (ContText.Text == "CLOSE")
            tween:Create(Blur, TweenInfo.new(0.4), {Size = 0}):Play()
            tween:Create(Overlay, TweenInfo.new(0.4), {BackgroundTransparency = 1}):Play()
            local out = tween:Create(UPanel, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Position = UDim2.new(0.5, -210, -1.5, 0), BackgroundTransparency = 1})
            out:Play()
            out.Completed:Connect(function()
                Blur:Destroy()
                UpdateGui:Destroy()
                if not isClosing then OnFinish() end
            end)
        end
    end)
end

--// 3. FUNGSI UTAMA (100% UTUH)
local function ExecuteMiyakoHub()
    local MiyakoLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Libry_New"))()
    local Window = MiyakoLib:Init()

    local runService = game:GetService("RunService")
    local uis = game:GetService("UserInputService")
    local isSpeedEnabled, isJumpEnabled, walkspeedValue = false, false, 16

    -- TABS (DEVELOPER: DEV MIYAKO)
    local MainTab = Window:NewTab("MAIN")
    local MovementSec = MainTab:NewSection("MIYAKO VIP")
    local MainSec = MainTab:NewSection("FITUR UTAMA")

    MovementSec:NewToggle("SPEED HACK", function(state)
        isSpeedEnabled = state
        if not state then pcall(function() LocalPlayer.Character.Humanoid.WalkSpeed = 16 end) end
    end)
    MovementSec:NewSlider("POWER SPEED", 16, 300, 16, function(v) walkspeedValue = v end)
    MovementSec:NewToggle("JUMP HACK", function(state) isJumpEnabled = state end)
    
    MovementSec:NewToggle("NO CLIP", function(state)
        if state then
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/No%20Clip"))()
        end
    end)

    MainSec:NewButton("MIYAKO STICK", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel"))()
    end)
    MainSec:NewButton("FLY V1", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tempel"))()
    end)
    MainSec:NewButton("FLY V2", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Fly%20V2"))()
    end)
    MainSec:NewButton("ANIMASI V1", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20V1"))()
    end)
    MainSec:NewButton("ANIMASI V2", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Animasi%20v2"))()
    end)
    MainSec:NewButton("ESP PRO", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Esp"))()
    end)
    MainSec:NewButton("AUTO AIM PRO", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Auto%20Aim"))()
    end)

    local BoxingTab = Window:NewTab("BOXING BETA")
    local CombatSec = BoxingTab:NewSection("COMBAT FEATURES")
    CombatSec:NewButton("MIYAKO XXV", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Box%20BB"))()
    end)
    CombatSec:NewButton("MIYAKO XXL", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/MIYAKO%20XXL"))()
    end)
    CombatSec:NewButton("BB V1", function()
        loadstring(game:HttpGet("https://rawscripts.net/raw/Boxing-Beta!-boxing-beta-fighting-speed-changer-88804"))()
    end)

    local ChaosTab = Window:NewTab("CHAOS")
    local ChaosSec = ChaosTab:NewSection("CHAOS FEATURES")
    ChaosSec:NewButton("TOUCH FLING", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling"))()
    end)
    ChaosSec:NewButton("TOUCH FLING V2", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Touch%20Fling%20V2"))()
    end)
    ChaosSec:NewButton("SPECT", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/spect"))()
    end)

    local TeleportTab = Window:NewTab("TELEPORT")
    local TeleportSec = TeleportTab:NewSection("TELEPORT FEATURES")
    TeleportSec:NewButton("MIYAKO MANAGER", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20manager"))()
    end)
    TeleportSec:NewButton("TP", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/Miyako%20Tp"))()
    end)

    local VdTab = Window:NewTab("VD")
    local VdSec = VdTab:NewSection("VD FEATURES")
    VdSec:NewButton("MOONWALK", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/moonwalk"))()
    end)

    local IndoTab = Window:NewTab("INDO HANGOUT")
    local IndoSec = IndoTab:NewSection("INDO HANGOUT FEATURES")
    IndoSec:NewButton("INDO HANGOUT V1", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/INDO%20HANGOUT"))()
    end)

    local BuildBaseTab = Window:NewTab("BUILD BASE &\nSTEAL")
    local BuildBaseSec = BuildBaseTab:NewSection("BASE & STEAL FEATURES")
    BuildBaseSec:NewButton("BUILD A BASE AND STEAL V1", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/SAGGSCRIPT/Build-a-base-and-steal-lag-fix/refs/heads/main/obfuscated.lua.txt"))()
    end)
    BuildBaseSec:NewButton("BUILD A BASE AND STEAL VIP", function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Miyako122/Miyako/refs/heads/main/BASE_BUILD_AND_STEAL"))()
    end)

    uis.JumpRequest:Connect(function()
        if isJumpEnabled then pcall(function() LocalPlayer.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end) end
    end)
    runService.Heartbeat:Connect(function()
        if isSpeedEnabled then
            pcall(function()
                local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
                if hum then hum.WalkSpeed = walkspeedValue end
            end)
        end
    end)
end

--// 4. EKSEKUSI
local updateData = GetUpdateData()
if updateData then
    ShowUpdateUI(updateData, ExecuteMiyakoHub)
else
    ExecuteMiyakoHub()
end
